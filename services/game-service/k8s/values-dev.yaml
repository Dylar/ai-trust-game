serviceName: game-service
namespace: atg-dev
environment: dev
component: backend
partOf: ai-trust-game

replicas: 1

image:
  repository: ghcr.io/dylar/ai-trust-game-game-service
  tag: local
  pullPolicy: IfNotPresent

imagePullSecrets: []

containerPort: 8080
servicePort: 8080

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 500m
    memory: 256Mi

config:
  APP_ENV: dev
  PORT: "8080"
  LLM_PROVIDER: static
  GROQ_MODEL: ""
