# Shared Audit Contracts

This package contains project-specific audit event contracts shared by services.

It is intentionally limited to the event and sink boundary needed by event producers.
Audit analysis, repositories, read models, and summarization belong to `services/audit-service/`.
