apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ai-trust-game-test

resources:
  - ../../base
  - namespace.yaml

images:
  - name: main-service
    newName: main-service
    newTag: test

patches:
  - path: config-map.patch.yaml
  - path: deployment.patch.yaml
