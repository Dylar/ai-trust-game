apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ai-trust-game-prod

resources:
  - ../../base
  - namespace.yaml

images:
  - name: main-service
    newName: ghcr.io/dylar/ai-trust-game-main-service
    newTag: latest

patches:
  - path: config-map.patch.yaml
  - path: deployment.patch.yaml
